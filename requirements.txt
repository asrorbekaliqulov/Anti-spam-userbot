# --- Core ---
Django==5.0.6
python-dotenv==1.0.1

# --- Telegram (userbot engine) ---
pyrogram==2.0.106
TgCrypto==1.2.5

# --- Hybrid filter / AI ---
openai==1.30.5
# openai 1.30.x passes the `proxies` kwarg to httpx, which was removed in
# httpx 0.28. Pin httpx to the last 0.27.x to avoid:
#   "AsyncClient.__init__() got an unexpected keyword argument 'proxies'"
httpx==0.27.2

# --- Image perceptual hashing (pHash) for GIF/photo spam ---
Pillow==10.3.0
imagehash==4.3.1

# --- QR-code rendering for userbot login ---
qrcode==7.4.2

# --- Async helpers ---
aiohttp==3.9.5

# --- Production server (optional) ---
gunicorn==22.0.0

# NOTE:
#   Pyrogram 2.x ships with its own MTProto implementation. TgCrypto is an
#   optional but strongly recommended C-extension that speeds up encryption.
